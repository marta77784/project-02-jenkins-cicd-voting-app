name: Build Result

on:
  push:
    branches:
      - 'main'
    paths:
      - 'result/**'
      - '.github/workflows/call-docker-build-result.yaml'
  pull_request:
    branches:
      - 'main'
    paths:
      - 'result/**'
      - '.github/workflows/call-docker-build-result.yaml'

jobs:
  build-only:
    name: Result Build (PR check)
    if: github.event_name == 'pull_request'
    uses: dockersamples/.github/.github/workflows/reusable-docker-build.yaml@main
    permissions:
      contents: read
      packages: write
      pull-requests: write
    with:
      dockerhub-enable: false
      ghcr-enable: false
      image-names: |
        ghcr.io/${{ github.repository_owner }}/voting-result
      tag-rules: |
        type=ref,event=pr
      context: result
      platforms: linux/amd64

  build-and-push:
    name: Result Build and Push (main)
    if: github.event_name == 'push'
    uses: dockersamples/.github/.github/workflows/reusable-docker-build.yaml@main
    permissions:
      contents: read
      packages: write
      pull-requests: write
    with:
      dockerhub-enable: false
      ghcr-enable: true
      image-names: |
        ghcr.io/${{ github.repository_owner }}/voting-result
      tag-rules: |
        type=raw,value=latest
        type=sha
      context: result
      platforms: linux/amd64
