name: Build Worker

on:
  push:
    branches:
      - 'main'
    paths:
      - 'worker/**'
      - '.github/workflows/call-docker-build-worker.yaml'
  pull_request:
    branches:
      - 'main'
    paths:
      - 'worker/**'
      - '.github/workflows/call-docker-build-worker.yaml'

jobs:
  build-only:
    name: Worker Build (PR check)
    if: github.event_name == 'pull_request'
    uses: dockersamples/.github/.github/workflows/reusable-docker-build.yaml@main
    permissions:
      contents: read
      packages: write
      pull-requests: write
    with:
      dockerhub-enable: false
      ghcr-enable: false
      image-names: |
        ghcr.io/${{ github.repository_owner }}/voting-worker
      tag-rules: |
        type=ref,event=pr
      context: worker
      platforms: linux/amd64

  build-and-push:
    name: Worker Build and Push (main)
    if: github.event_name == 'push'
    uses: dockersamples/.github/.github/workflows/reusable-docker-build.yaml@main
    permissions:
      contents: read
      packages: write
      pull-requests: write
    with:
      dockerhub-enable: false
      ghcr-enable: true
      image-names: |
        ghcr.io/${{ github.repository_owner }}/voting-worker
      tag-rules: |
        type=raw,value=latest
        type=sha
      context: worker
      platforms: linux/amd64
